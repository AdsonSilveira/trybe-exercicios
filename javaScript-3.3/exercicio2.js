//let word = 'tryber';

 //for (let reverse = str.length = 1; reverse-= 1) { 
    //    word += str[reverse];        
//}
//console.log(word); 

var word = "trybe";
 
for (var index = word.length - 1; index >= 0; i--) {
    console.log(word[index]);
}